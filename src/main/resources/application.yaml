spring:
  application:
    name: shopper
  datasource:
    url: jdbc:h2:mem:demo;DB_CLOSE_DELAY=-1
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  cache:
    type: simple
  mail:
    host: smtp.gmail.com
    port: 587
    username: ichwansholihin70@gmail.com
    #password: https://myaccount.google.com/apppasswords?spm=a2ty_o01.29997173.0.0.35245171Ol7HSF (don't use space)
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

#redis:
#  host: localhost
#  port: 6379
#  timeout: 2000
#  jedis:
#    max-total: 50
#    max-idle: 20
#    min-idle: 5

#cache:
#  product:
#    ttl-seconds: 600  # 10 minutes

  # Session Configuration
  session:
    store-type: redis
    timeout: 1800s # 30 menit
    redis:
      namespace: spring:session # prefix untuk Redis keys
      flush-mode: on_save # immediate atau on_save
      cleanup-cron: "0 * * * * *" # cleanup expired sessions setiap menit
